import type { OutputKind } from '../../types.mts';
export declare function handleManifestConda({ cwd, filename, out, outputKind, verbose }: {
    cwd: string;
    filename: string;
    out: string;
    outputKind: OutputKind;
    verbose: boolean;
}): Promise<void>;
//# sourceMappingURL=handle-manifest-conda.d.mts.map