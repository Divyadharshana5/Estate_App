import type { CResult } from '../../types.mts';
import type { SocketSdkReturnType } from '@socketsecurity/sdk';
export declare function fetchListScans({ branch, direction, from_time, orgSlug, page, per_page, repo, sort }: {
    branch: string;
    direction: string;
    from_time: string;
    orgSlug: string;
    page: number;
    per_page: number;
    repo: string;
    sort: string;
}): Promise<CResult<SocketSdkReturnType<'getOrgFullScanList'>['data']>>;
//# sourceMappingURL=fetch-list-scans.d.mts.map