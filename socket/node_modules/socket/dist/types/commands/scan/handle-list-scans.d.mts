import type { OutputKind } from '../../types.mts';
export declare function handleListScans({ branch, direction, from_time, orgSlug, outputKind, page, per_page, repo, sort }: {
    branch: string;
    direction: string;
    from_time: string;
    orgSlug: string;
    outputKind: OutputKind;
    page: number;
    per_page: number;
    repo: string;
    sort: string;
}): Promise<void>;
//# sourceMappingURL=handle-list-scans.d.mts.map