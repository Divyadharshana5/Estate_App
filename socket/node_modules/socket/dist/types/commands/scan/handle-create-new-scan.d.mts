import type { OutputKind } from '../../types.mts';
export declare function handleCreateNewScan({ autoManifest, branchName, commitHash, commitMessage, committers, cwd, defaultBranch, interactive, orgSlug, outputKind, pendingHead, pullRequest, readOnly, repoName, report, targets, tmp }: {
    autoManifest: boolean;
    branchName: string;
    commitHash: string;
    commitMessage: string;
    committers: string;
    cwd: string;
    defaultBranch: boolean;
    interactive: boolean;
    orgSlug: string;
    pendingHead: boolean;
    pullRequest: number;
    outputKind: OutputKind;
    readOnly: boolean;
    repoName: string;
    report: boolean;
    targets: string[];
    tmp: boolean;
}): Promise<void>;
//# sourceMappingURL=handle-create-new-scan.d.mts.map