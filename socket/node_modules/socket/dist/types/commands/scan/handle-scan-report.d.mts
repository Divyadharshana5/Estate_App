import type { OutputKind } from '../../types.mts';
export declare function handleScanReport({ filePath, fold, includeLicensePolicy, orgSlug, outputKind, reportLevel, scanId, short }: {
    orgSlug: string;
    scanId: string;
    includeLicensePolicy: boolean;
    outputKind: OutputKind;
    filePath: string;
    fold: 'pkg' | 'version' | 'file' | 'none';
    reportLevel: 'defer' | 'ignore' | 'monitor' | 'warn' | 'error';
    short: boolean;
}): Promise<void>;
//# sourceMappingURL=handle-scan-report.d.mts.map