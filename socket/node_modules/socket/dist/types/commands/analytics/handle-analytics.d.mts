import type { OutputKind } from '../../types.mts';
export declare function handleAnalytics({ filePath, outputKind, repo, scope, time }: {
    scope: string;
    time: number;
    repo: string;
    outputKind: OutputKind;
    filePath: string;
}): Promise<void>;
//# sourceMappingURL=handle-analytics.d.mts.map