import type { OutputKind } from '../../types.mts';
import type { LocalConfig } from '../../utils/config.mts';
export declare function handleConfigSet({ key, outputKind, value }: {
    key: keyof LocalConfig;
    outputKind: OutputKind;
    value: string;
}): Promise<void>;
//# sourceMappingURL=handle-config-set.d.mts.map