import type { FixConfig } from './agent-fix.mts';
import type { OutputKind } from '../../types.mts';
import type { Remap } from '@socketsecurity/registry/lib/objects';
export type HandleFixConfig = Remap<FixConfig & {
    ghsas: string[];
    outputKind: OutputKind;
    unknownFlags: string[];
}>;
export declare function handleFix({ autoMerge, cwd, ghsas, limit, minSatisfying, outputKind, prCheck, purls, rangeStyle, spinner, test, testScript, unknownFlags }: HandleFixConfig): Promise<void>;
//# sourceMappingURL=handle-fix.d.mts.map