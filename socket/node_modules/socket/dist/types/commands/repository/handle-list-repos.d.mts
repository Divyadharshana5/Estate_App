import type { OutputKind } from '../../types.mts';
export declare function handleListRepos({ all, direction, orgSlug, outputKind, page, per_page, sort }: {
    all: boolean;
    direction: 'asc' | 'desc';
    orgSlug: string;
    outputKind: OutputKind;
    page: number;
    per_page: number;
    sort: string;
}): Promise<void>;
//# sourceMappingURL=handle-list-repos.d.mts.map